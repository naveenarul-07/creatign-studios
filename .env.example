# Server
PORT=5000
NODE_ENV=development

# Optional MongoDB connection. When omitted, the API uses in-memory data.
MONGODB_URI=

# CORS origin for the frontend in development
CLIENT_ORIGIN=http://localhost:5173

# Frontend (Vite)
VITE_API_URL=/api
